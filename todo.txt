JSON insertion
appending text
when setting vars, accept array of var targets
support express calling of scripts (may not be necessary considering terminal command)
add scaled logging class, so logs can be controlled via a setting
make update-file create a file if one doesn't exist
add if directive
add while directive
Combine write-file and update-file into a single action type
Make do until (time)
Fix and finish email (fix/check send, add attachments)
add time to logs
fix xml conversion
Allow value substitution on the operations objects
Add force type to variable increments, to fix issue where a variable in the config concats instead of increments
